import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kinga.paikady',
  appName: 'Paikady',
  webDir: '../tasks-service/src/main/resources/static'
};

export default config;
